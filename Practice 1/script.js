let cup = 5;
console.log(cup);

cup += 5;
console.log(cup);

const add_this = 2;

function incrementNumber(initial,incrementor) {
    return initial + incrementor;
}

console.log(incrementNumber(cup, add_this))
